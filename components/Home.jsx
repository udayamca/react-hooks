export default function Home() {
  return (
    <>
      <div>
        <h1>React Hooks</h1>
        <div>
          React Hooks are functions introduced in React that allow functional
          components to use state, lifecycle features, context, refs, and other
          React capabilities without needing class components. Hooks make
          components cleaner, more reusable, and easier to manage by organizing
          logic based on functionality instead of lifecycle methods.
        </div>
      </div>
    </>
  );
}
